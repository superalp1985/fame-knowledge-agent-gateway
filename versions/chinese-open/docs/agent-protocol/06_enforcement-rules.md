# 06 强制规则

- 不允许全量加载知识网。
- 不允许把完整日志塞进上下文。
- 不允许未看说明就调用陌生工具。
- 不允许没有球门就扩展上下文。
- 不允许没有工具动作契约就执行风险工具。
- 不允许未验证就宣称完成。
- 不允许把工程 overlay 自动并入核心知识。
- 不允许把失败抹掉。

失败、部分成功或用户纠偏后，要写 failure signature，并给出负值 FAME 候选。
