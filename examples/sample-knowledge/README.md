# Sample Knowledge Root

This is a tiny public sample. The open-source knowledge base lives in `knowledge/`.

Private local experiments and unsanitized backups should stay in `knowledge_backup/` and should not be committed.

## Route

```text
concept -> association -> abstraction -> rule -> tool -> quality gate
```
